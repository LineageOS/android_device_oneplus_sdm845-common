android_app {
    name: "OnePlusDiracGef",

    asset_dirs: ["assets"],
    srcs: ["src/**/*.kt"],

    certificate: "platform",
    platform_apis: true,

    optimize: {
        proguard_flags_files: ["proguard.flags"],
    },
}
